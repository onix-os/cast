let a = import! cast.authored.v1

\deps ->
    {
        meta = {
            pname = "override-client",
            version = "8.2.0",
            release = 1,
            homepage = "https://example.invalid/override-client",
            license = ["MPL-2.0"],
        },
        builder = a.builder.cmake { flags = ["-DUSE_SYSTEM_LIBRARIES=ON"], run_tests = a.true },
        sources = [
            a.source.archive
                "https://example.invalid/override-client-8.2.0.tar.xz"
                "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
        ],
        native_build_inputs = [],
        build_inputs = [deps.compression, deps.tls],
        check_inputs = [],
        outputs = a.outputs.default,
        options = a.unset,
        profiles = [],
        architectures = [],
        tuning = [],
        emul32 = a.false,
        mold = a.false,
        hooks = a.unset,
    }
