let a = import! cast.authored.v1

{
    meta = {
        pname = "multi-source-app",
        version = "6.0.1",
        release = 1,
        homepage = "https://example.invalid/multi-source-app",
        license = ["MPL-2.0"],
    },
    builder = a.builder.meson {
        flags = [],
        run_tests = a.true,
    },
    sources = [
        a.source.archive_with {
            url = "https://example.invalid/multi-source-app-6.0.1.tar.xz",
            hash = "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
            rename = a.optional.set "application.tar.xz",
            strip_dirs = a.optional.set 1,
            unpack = a.true,
            unpack_dir = a.optional.set "application",
        },
        a.source.git_with {
            url = "https://example.invalid/vendor-protocol.git",
            git_ref = "0123456789abcdef0123456789abcdef01234567",
            clone_dir = a.optional.set "vendor-protocol",
        },
        a.source.archive_with {
            url = "https://example.invalid/generated-schema.dat",
            hash = "abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789",
            rename = a.optional.set "schema.dat",
            strip_dirs = a.optional.unset,
            unpack = a.false,
            unpack_dir = a.optional.unset,
        },
    ],
    native_build_inputs = [],
    build_inputs = [],
    check_inputs = [],
    outputs = a.outputs.default,
    options = a.unset,
    profiles = [],
    architectures = [],
    tuning = [],
    emul32 = a.false,
    mold = a.false,
    hooks = a.unset,
}
