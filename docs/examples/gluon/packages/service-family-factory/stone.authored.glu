let a = import! cast.authored.v1
let family = import! "./family_authored.glu"
let make_member = import! "./package_authored.glu"
let release = import! "./release.glu"

// One release record feeds every member, while this declaration selects only
// the daemon closure and its service/configuration output contract.
make_member release family.daemon
