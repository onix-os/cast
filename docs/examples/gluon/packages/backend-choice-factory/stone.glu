let factory = import! "./package.glu"

// A closed variant makes the three backends mutually exclusive. Adding a
// backend requires extending the factory match instead of coordinating
// several independent booleans.
factory.make factory.libressl
