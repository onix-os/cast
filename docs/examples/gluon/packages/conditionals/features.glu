
let a = import! cast.authored.v1

// Feature switches are typed values consumed by an ordinary pure function.
// They can select complete list fields without stringly conditional syntax.
{
    documentation = a.true,
    tests = a.true,
    tls = a.false,
}
