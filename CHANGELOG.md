# Changelog

## [0.27.0] - 2026-07-23

### .github

- Check REUSE compliance in parallel to code conformity
- Add CODEOWNERS file

### <!-- 0 -->⛰️  Features

- Hmmmmmm
- Hmmmmmm

### <!-- 1 -->🐛 Bug Fixes

- Execute relation policy and genuine PGO
- Bound nested campaign runtimes
- Patch extracted sources from frozen cwd
- Route generated debug output explicitly
- Serialize evidence lifecycle
- Validate vendored Cargo checksums
- Close Go module fallback paths
- Make Go builds proc-independent
- Keep local policy available outside tests
- Add offline Python module recipe
- Prove pinned offline Go module
- Prove deterministic font family package
- Prove declarative desktop integration
- Split bootstrap closure modules
- Prove gettext localization package
- Prove declarative system integration assets
- Prove mixed locked source composition
- Prove staged header-only package
- Prove staged post-install execution
- Regenerate real Autotools build
- Prove Meson dependency roles
- Exercise declared Zlib build input
- Execute independently locked patch source
- Interrupt delegated status waits safely
- Seal authenticated execution proof
- Make CI finalization signal-interruptible
- Prove generated shell and plugin outputs
- Admit userspace profile to execution matrix
- Typo in moss remove help

### <!-- 2 -->🚜 Refactor

- Share canonical journal reopen
- Isolate one-shot usr exchange syscall
- Split startup gate Make contract
- Split startup database evidence
- Split staging wrapper lifecycle
- Split policy and fingerprint boundaries
- Split Mason root profile and Git boundaries
- Split client and build explanation boundaries

### <!-- 3 -->📚 Documentation

- Close merged development plan
- Record deferred closure evidence
- Defer remaining activation campaign
- Correct installed receipt lifecycle
- Record forward active reblit finalization
- Record active reblit completion edge
- Record active reblit cleanup completion
- Record startup boot completion adoption
- Record durable boot sync completion
- Record terminal receipt promotion authority
- Record exact receipt promotion boundary
- Record aggregate GPT publication evidence
- Record real GPT boot campaign
- Record VM boot leaf publication
- Record disposable VM VFAT campaign
- Record VM boot staging evidence
- Record disposable VM recovery evidence
- Record strict startup receipt state
- Record complete boot receipt foundation
- Record boot receipt correlation boundary
- Retain deferred roadmap work
- Record complete fixture campaign
- Retain deferred design alternatives
- Defer host temporary-root cleanup
- Defer maintenance and VM tooling
- Record deferred work
- Record RootLinks restart recovery
- Record RootLinks ActiveReblit finalization
- Record RootLinks NewState finalization
- Record interrupted journal recovery
- Record archived RootLinks finalization
- Narrow configuration format audit
- Record bound journal deletion
- Record RootLinks NewState completion
- Record RootLinks invalidation SIGKILL proof
- Record RootLinks NewState invalidation
- Record RootLinks ActiveReblit completion
- Rebrand as Cast, add Nix comparison
- README
- Record RootLinks archived completion
- Record RootLinks NewState routing
- Record RootLinks candidate preservation
- Record exact candidate persistence
- Distinguish relations from dependency roles
- Bound fixture failure claims
- Record fixture guest capacity failure
- Define destructive VM readiness proof
- Record exact candidate record authority
- Record bounded fixture rerun contract
- Record exact root ABI rollback reversal
- Record exact root ABI rollback routing
- Record durable root ABI startup recovery
- Record root ABI completion boundary
- Record interrupted root ABI recovery
- Correct boot repair route status
- Record anchored system trigger view
- Define private minimal device boundary
- Record delegated vm execution boundary
- Record desired boot inventory milestone
- Record sha256 boot identity milestone
- Record immutable boot payload milestone
- Record boot namespace binding milestone
- Record retained boot composition progress
- Record retained read evidence progress
- Record block and directory evidence foundations
- Record stable GPT table evidence
- Record boot namespace assessment
- Record strict partition evidence
- Record composed boot filesystem evidence
- Record descriptor filesystem evidence
- Record boot publication prerequisites
- Align boot module status
- Record deterministic boot rendering
- Record pure render input boundary
- Record one-deadline boot foundations
- Refresh activation recovery status
- Define mounted boot topology authority
- Record Go and drafting hardening
- Record font and recovery evidence
- Record desktop and NewState recovery proofs
- Record gettext localization fixture
- Record archived preservation kill matrix
- Record declarative system assets proof
- Record archived candidate-move SIGKILL proof
- Correct archived recovery inventory
- Record archived terminal SIGKILL proof
- Record mixed locked source fixture
- Record archived terminal finalization
- Record archived completion dispatch
- Record archived production preservation
- Record ActivateArchived sealed foundations
- Record terminal SIGKILL recovery proof
- Record production rollback finalization
- Record production new-state recovery suffix
- Record rollback completion routing
- Record fresh database invalidation persistence
- Record fresh database invalidation authority
- Record exact fresh transition removal
- Record fresh database invalidation routing
- Record candidate preservation persistence
- Record candidate post-move durability
- Record candidate move target durability
- Record durable candidate target normalization
- Record one-shot candidate target creation
- Record target preparation lease boundary
- Record candidate target hardening
- Record new state preservation move
- Record candidate preservation admission
- Split state activation recovery plan
- Record candidate intent routing
- Record process crash recovery coverage
- Record reverse recovery hardening
- Record rollback resume routing
- Record usr exchange durability recovery
- Record journal-only rollback decisions
- Record authorized startup residue recovery
- Record userspace execution capability evidence
- Record one-shot usr exchange proof
- Split state recovery subplan
- Record retained state authorities
- Record post-gate intent evidence
- Correct remaining hardening evidence
- Describe complete container credentials
- Clarify example execution proof
- Model native generators and git subprojects
- Model declarative package-set composition
- Keep header include flags typo-clean
- Exercise typed package-set semantics
- Expand the declarative package corpus
- Add layered offline package examples
- Add offline Go Zig and gettext recipes
- Keep future Nix interoperability open
- Make frozen step isolation explicit
- Leave Nix interoperability undecided
- Define durable activation tree identity
- Record activation journal foundations
- Plan crash-safe state activation
- Expand Gluon package recipe patterns
- Record completed execution hardening gates
- Add Stone-native packaging examples
- Leave future Nix interoperability open
- Define Stone-native composition model
- Keep Nix as recipe inspiration only
- Point cast installs at the fork documentation
- Make cast the sole public product
- Define the cast one-binary architecture
- Explain typed build input provenance
- Specify byte-locked Git sources
- Finish the package v3 hard fork
- Clarify lock and package ABI ownership
- Correct package output ownership
- Describe declarative policy composition
- Document literal shell build context
- Retire internal recipe model
- Mark policy layers complete
- Document frozen package workflow
- Record declarative Stone progress
- Plan declarative Stone packages
- Expand upstream acknowledgments
- Explain the Onix hard fork
- Refresh paths after workspace cleanup
- Thank the original OS Tools authors
- Complete the Gluon migration plan
- Record Gluon target compatibility
- Explain explicit source lock refresh
- License the Gluon migration plan
- Record final Gluon linkage impact
- Demonstrate typed recipe composition
- Define the Gluon configuration contract
- Plan Gluon configuration migration
- Install intructions for `typos-cli`

### <!-- 5 -->🎨 Styling

- Format ActiveReblit recovery tests

### <!-- 6 -->🧪 Testing

- Add receipt-bound aggregate GPT campaign
- Bind GPT runner build root exactly
- Harden disposable GPT boot campaign
- Admit empty publisher manifest staging
- Validate VM publisher artifact filter
- Validate Nix develop profile file
- Address staged boot flake explicitly
- Stage VM publisher source as root
- Build VM boot publisher before disk effects
- Admit trusted guest tool symlinks
- Publish retained boot leaf in VM
- Harden disposable VM VFAT campaign
- Guard disposable VM boot storage
- Restore active reblit dispatch inventory
- Restore archived completion inventory
- Restore complete new-state suffix inventory
- Follow renamed rollback recovery helper
- Narrow journal binding clone guards
- Lock ActiveReblit RootLinks coverage
- Refresh NewState dispatch ownership gates
- Refresh rollback fixture ownership gates
- Align coordinator recovery with root ABI normalization
- Repair direct Forge inventory gate
- Fix go fixture provider proof
- Fix python fixture provider proof
- Harden pinned python fixture host
- Isolate bundled git fixture from host config
- Cover anchored bind clone classification
- Preserve exact erofs through fs-err context
- Complete synthetic partition identity
- Refresh retained boot topology fixtures
- Pin GPT table fingerprint v1
- Isolate fixture tool invocations
- Assert canonical template tool paths
- Supervise checked-in example children
- Bound checked-in example gates
- Allow bounded cold test discovery
- Keep mountinfo compatibility synthetic
- Forbid local host storage access
- Bound archived prune inventory
- Release restart lock capabilities
- Require a real boot projection
- Stabilize recovery capability baselines
- Admit pinned libfyaml fixture metadata
- Prove terminal rollback SIGKILL recovery
- Expose terminal journal delete boundaries
- Admit new-state target preparation
- Avoid argv limits in inventory gates
- Kill journal updates at durable boundaries
- Kill reverse recovery at process boundaries
- Expose journal update kill boundaries
- Replace fixture timing races with gates
- Require metadata provenance coverage
- Pin hardened fixture metadata
- Keep frozen destinations disjoint
- Cover every delegated fixture selector
- Freeze layered Gluon recipe semantics
- Review Nix scope documentation
- Preserve fixture declaration inputs
- Pin each execution fixture closure
- Separate offline bootstrap execution
- Keep contentful fixtures offline
- Reject metadata-only execution closures
- Prove canonical execution bundles
- Decode real execution bundles
- Reuse root index wire constant
- Scope Git descriptor leak checks
- Keep sandbox target attached to policy
- Move fixtures under tests
- Preserve the authored source fixture
- Keep transaction inhibitor seam warning-free
- Preserve intent during repository suggestions
- Reject imperative repository changes
- Exercise Gluon sync import
- Exercise snapshot-preserving verification
- Exercise state creation and export
- Record recipe composition primitives
- Keep Gluon examples executable
- Migrate recipe fixtures to Gluon
- Update triple

### <!-- 7 -->⚙️ Miscellaneous Tasks

- Split Make recipes into functional modules
- Reconcile receipt-owned boot cleanup after restart
- Declare explicit interpreters
- Bound fixture execution capacity
- Enforce roles and reject special inodes
- Declare ninja shell interface
- Preserve compiler argument multiplicity
- Freeze Autotools child tools
- Seal locale inputs across services
- Pin evidence runner Bash
- Align build policy ABI marker
- Require explicit topology mount selectors
- Bound declarative recipe drafting
- Preflight delegated execution before bootstrap
- Require fixture host tools explicitly
- Reconcile exact fresh transition removal
- Reconcile candidate target mode semantics
- Reconcile absent candidate target creation
- Reconcile new state candidate preservation
- Reconcile one-shot reverse effect
- Persist authorized usr rollback decisions
- Preserve intentional header removals
- Harden fixture lifecycle ownership
- Publish bounded fixture execution evidence
- Split build lock codec and validation
- Split derivation model and tests
- Split build policy validation
- Split package validation
- Extract derivation validation
- Extract derivation provenance
- Validate develop fixture gates
- Normalize fork-authored file starts
- Remove false upstream attribution
- Extract locked archives structurally
- Make real recipe fixtures mandatory
- Declare autotools install program
- Require frozen fixture execution
- Use official root index wire name
- Bound evaluated package resources
- Fail closed on malformed package declarations
- Enforce the cast public naming boundary
- Validate complete source declarations
- Clarify the retired recipe domain
- Close Gluon-only format gate bypasses
- Run the canonical Makefile validation
- Prove configuration formats stay Gluon-only
- Name tuning selections explicitly
- Use explicit build context variables
- Remove legacy recipe model
- Isolate output policy templates
- Expose direct package semantics
- Harden frozen plan validation
- Remove legacy recipe ABI
- Add structured package builders
- Load explicit Gluon policy root
- Add canonical derivation plans
- Add typed package v2 ABI
- Remove funding metadata
- Remove REUSE integration
- Finish root file cleanup
- Organize docs and ancillary assets
- Validate package relations
- Refresh source locks from authored recipes
- Replace control overlays with composition
- Record recipe evaluation provenance
- Remove legacy format dependencies
- Remove YAML and KDL compatibility
- Remove YAML recipe compatibility
- Make recipe tooling Gluon-native
- Encode canonical Gluon recipes
- Remove YAML macro reader
- Drop direct YAML and KDL dependencies
- Add Gluon macro policy ABI
- Add format-neutral macro specifications
- Add Gluon recipe check command
- Load canonical stone.glu recipes
- Evaluate versioned Gluon specifications
- Import recipe ABI through policy
- Evaluate versioned Gluon recipe ABI
- Reject ambiguous recipe formats
- Use shared recipe validation
- Add format-neutral system specification
- Add format-neutral recipe specification
- Fix `state verify` hash padding mismatch
- Add ent reqwest support for recipe updates
- Colorize recipe update diff output
- Remove std{in/out} stream for recipe up
- Overwrite unconditionally w/ -y
- Refactor recipe cli for release_series
- Tweak recipe help text, fix FTBFS
- Diff & confirm in all cases in recipe update
- Allow writing recipe to another path than input
- Update stdin/override defaults for recipe update
- Add support for autoupdating a recipe
- Add progressbar & tracing output to prune
- Data: cmake: Escape quotes around "Unix Makefiles"
- Draft: Add current year in copyright header for new recipes
- Add control file decoding
- Add ControlFile::apply_to_recipe helper
- Add control file types
- Only attempt to --cleanup paths that exist
- Guess new ver when updating recipe if no ver is provided
- Fix user facing TODO comment
- Only attempt to --cleanup paths that exist
- Search for binaries ([#724](https://github.com/bresilla/roc/issues/724))
- Make it portable to /bin/sh using distros
- Upstream: Fix deserialization of the "unpackdir" property
- Recipe: Rename enums involved in the update subcommand
- Use the new SourceUri struct from stone_recipe
- Move upstream-related types into the upstream module
- Deserialize Upstream using SourceUri
- Add the SourceUri struct for URLs
- Ensure recipe update always quotes version
- Fix clippy lints
- Cache upstreams fetched via recipe new & update
- Support extracting to specified output dir
- Check for int in version string first char
- Swap BTreeSet for HashSet in UnpackingInProgress
- Add system model MVP
- Add global cargo cache
- Update to resolver = "3"
- No need to drop guard manually
- Refactor progress tracing to client layer
- Add tracing to remove command
- Document recipe invariant checks
- Ensure recipe release is positive
- Tweak %export_docbook_catalogs action
- Use in_current_span for async tracing instrumentation
- Add tracing to sync command
- Change recipe template to © AerynOS
- Move event macros for progress spans out of helper functions, remove progress module from tracing_common
- Simplify instrument decorators, remove useless display formatting in tracing macros
- Rename progress event type
- `moss install` better progress tracing
- Reorganize tracing utilities and add progress spans for moss install
- Disable ANSI output for file logging in tracing subscriber so we don't log ANSI color codes to files
- Enhance tracing and instrument `moss install` command and its call stack
- Add tracing_common crate and initalize tracing in moss CLI
- Normalize Python providers/dependencies properly
- Remove cache from release workflow
- Rename some things
- Update "nightly" release workflow to run on tag push
- Use more specialized methods from petgraph
- Add %(completionsdir) for rebuild compat ([#485](https://github.com/bresilla/roc/issues/485))
- Normalize python providers
- Add specific %(*completionsdir) definitions
- Add new ReplaceFile enum option in Decision
- Use bash for packaging phases in stone.yaml
- Respect transaction specific schemas
- Allow actually removing the new flags
- Handle zip vs tar origins for `boulder new`
- Write monitoring.yaml file attempting to match fields
- Default to autotools on unhandled build system
- Fix github matching
- Normalize all python providers to lowercase
- Update crate-ci/typos
- Enable automatic rollback boot menus
- Add back umask after pivot_root
- Allow skipping triggers for moss state activate
- Allow suppression of recipe bump in updates
- Allow RPATH dependencies across package boundaries
- Turns out that `tar` is a hard requirement
- Prevent soname clashes from private/rpath exports
- Reduce allocations by setting initial capacity (prevent resize)
- Temporarily exclude curl chain dependencies
- Add support for automatic python dependencies
- Add get-py-deps.py for python dependency analysis
- Set CPP env to clang-cpp for llvm toolchain
- Add shorthand aliases for boulder recipe {bump,update}
- Add python-packaging as a dep for setup.py
- Add versioned python providers
- Ensure localhost networking is setup
- Ensure we never hit 0 for column width limits
- Fix sync transaction & use pinned providers
- Update GNU toolchain packages
- Use more sophisticated cache action
- Fix racey seek on content file
- Adjust cargo_* macros
- Add cmake and pkgconf dependencies to meson setup macro
- Export current PGO stage into environment
- Use shared moss root in boulder cache
- Fix emul32 incompatibility with dlang boulder
- Tweak boulder concurrency recipe + README
- Update README, add concurrency test recipe
- Cleanup dependencies
- Implement recipe bump support
- Specify RUSTFLAGS directly to cargo
- Add RUSTFLAGS, enable frame pointers by default
- Switch to deterministic BTreeMap/BTreeSet
- Ensure boulder alias is specified properly
- Add description & example fields to macro actions
- Add default rust flags
- Add link to documentation site
- Enable clippy for workspace level
- Enable typo checking cuz we can't spell gud
- Move new command under recipe and alias to boulder new
- Resolve recipe path and call it when loading recipe
- Form full absolute path for recipes
- Add -b flag to specify build release number
- Ensure gcc's lto is parallelized
- Resolve recipe path when provided a directory
- Rework to ensure we compile early and order correctly
- Bump the cargo group with 13 updates
- Detect dependencies/provides based on meta tag
- Ensure `ci` runs `--all` cargo tests
- Bump the cargo group with 7 updates
- Bump the cargo group with 3 updates
- Bump the cargo group with 4 updates
- Bump the cargo group with 2 updates
- For installed packages, mark explicit selections in bold
- Bump the cargo group with 1 update
- Bump the cargo group with 3 updates
- Specify the need to create the sosroot/ dir
- Bump the cargo group with 1 update
- Bump the cargo group with 4 updates
- Avoid adding cyclical deps
- Bump the cargo group with 3 updates
- Bump the cargo group with 2 updates

### CODEOWNERS

- No point in expecting Ikey to review
- Update to individual contributors
- Add tooling:core group for reviews
- Add tarkah so he can approve PRs

### Cargo

- Bump to version 0.25.5
- Use bugfixed elf upstream
- Sync blsforme for loader.conf generation
- Sync os-info/blsforme
- Sync blsforme and others
- Bump 0.25.1
- Sync blsforme for nvidia fixes
- Sync blsforme for autoremoval support
- Sync blsforme to enable per-kernel cmdlines
- Sync blsforme for kernel cmdline support
- Sync new blsforme for quiet cmdlines
- Run `update`
- Sync blsforme to fix inability to mount ESP
- Sync blsforme with git main to fix vfat mount options (more secure)
- Update blsforme to ensure non xbootldr systems work
- Update blsforme to get "rw" parameter, making systems bootable
- Follow blsforme main branch
- Overdue update
- Updates
- Add dialoguer for input processing
- Add remaining updates
- Update deps that dependabot missed
- Update
- Dependanot
- Update the others
- Add a proper packaging build config
- Update deps
- Sync lock
- Stick ratatui in the global workspace, not tui
- Add `diesel-rs` with `sqlite` for future DB
- Add `clap` for lovely CLI processing

### Cargo.lock

- Update it (again)
- Update for Cargo.toml version 0.25.5
- Sync
- Before dependabot nags me
- Update for workspace
- Cargo update

### Cargo.toml

- Update MSRV to 1.88 for let-chains
- Set opt-level=2 for onboarding target
- Add thinlto to release builds

### DESIGN-NOTES

- Improve headers and formatting slightly

### DESIGN-NOTES.md

- Initial commit for AerynOS crash course

### Global

- Rename to mod.rs where needed

### LICENSES

- Include licence for MPL-2.0

### README

- A few more tweaks for consistency
- Use aosroot and use unstable stream from cdn
- Update with runtime prerequisites
- Update a few references to Aeryn OS where applicable
- *shazam*
- Update `just get-started` prerequisites
- User namespace building needs --moss-root
- Fix typo
- Add boulder alias example for building
- Add note on /etc/{subuid,subgid} support
- We no longer support PREFIX installs
- Installing to ${HOME}/.local by default now
- Update instructions on using `cargo test --all`
- Update
- We haz triggers
- Mark two more done
- Another one bites the dust
- Hey, @ermo
- Transactions exist
- Ensure we know the contribution process
- Ok half done isn't enough
- Document our steps
- Details

### README.md

- Use AerynOS's resources in get-started's script
- Update onboarding (moss -> tools)
- Recommend $HOME/.config/boulder for boulder --config-dir
- Update onboarding to make more sense

### YAML

- Allow only 'true' and 'false' per YAML 1.2

### All

- Add serpent_buildinfo crate and use it from boulder/moss
- Set MSRV to 1.78
- Derive version from workspace
- Use stdlib OnceLock for HTTP client
- Address all encountered typos
- Apparently none of us can spell library. Self fulfilling prophecy confirmed.

### Analyzers

- Bound subprocess resources

### Astr

- Use ThinArc

### Autotools

- Remove %(options_configure) in %autogen*

### Boot

- Fingerprint canonical desired publications
- Bind publications to sha256 content identity
- Checksum-address immutable payload paths
- Compose descriptor filesystem topology evidence
- Retain closed mount policy evidence
- Render deterministic BLS requests
- Preserve render asset lifetimes
- Retain topology render deadline
- Bind pure render inputs
- Preserve Stone input deadlines
- Preserve state root deadlines
- Preserve render input deadlines
- Authenticate machine root intent
- Scope publication collisions by topology
- Prove interrupted cmdline read bounds
- Authenticate package cmdline inputs
- Declare machine-local topology intent
- Authenticate state schema inputs
- Retain local cmdline policy
- Retain interrupted repair attempts
- Retain authenticated state roots
- Bind publication roles safely
- Separate generated schema requirements
- Bound deterministic publication plans
- Bind coherent Stone repair inputs
- Plan bounded Stone boot inputs
- Seal authenticated CAS inputs
- Freeze deterministic database projection
- Require clean authority for manual repair
- Port moss-fstx to use `--skip-triggers`
- Make sure we don't allow any questions... :)
- Fix typo in systemd unit
- Begin work on dracut integration for system rollback

### Boulder

- Remove ambient scheduler policy
- Execute locked root materialization
- Explain complete frozen plans
- Explain ordered policy provenance
- Make typed targets authoritative
- Delete legacy macro language
- Retire macro phase expansion
- Add finite typed build context
- Resolve build root from typed policy
- Distinguish the second PGO stage
- Resolve tuning from typed policy
- Remove legacy macro inspection command
- Freeze typed build steps directly
- Resolve package outputs directly
- Plan from direct package specs
- Expose ordered policy layers
- Remove legacy execution paths
- Execute frozen derivation plans
- Isolate analysis and source inputs
- Isolate frozen container inputs
- Package from frozen plans
- Materialize frozen sources
- Freeze package execution inputs
- Verify and install locked roots
- Record derivation identity
- Freeze canonical derivation plans
- Execute structural package steps
- Lock exact build closures
- Make package v2 canonical
- Define declarative plan boundaries
- Reject invalid package relations
- Prove migrated macro policy equivalence
- Refresh moving Git source locks
- Restore macro module licensing
- Shrink build CLI errors
- Box Gluon configuration errors
- Migrate profiles to Gluon
- Stop applying KDL control files
- Migrate macro data to Gluon
- Consume generated Gluon source locks
- Add canonical Gluon source lock
- Support package-private runtime closures
- Fix emul32 builds
- Add Zig caches
- Add GOFLAGS
- Add verbosity to make
- Add VALAFLAGS
- Initial support for rust pgo
- Update to new kdl profile
- Hook into outdated repo update function
- Enable adding / listing root index repos
- Fix submodules
- Fix small typo in previous build.rs commit
- Tweak build thread priority error message
- Robustify thread priority setter for build
- Hide debug output behind verbose flag
- Fix macros with bash nounset
- Upstream: Remove Shared structs
- Clean up shared sources when a cleanup is requested
- Upstream: Adapt to recent Git and Plain
- Upstream: plain: Refactor
- Upstream: git: Refactor
- Use tempdir for container root when cleaning cache
- Fix moss system root path check
- Add more bash safety
- Add provides-excludes
- Add cmake/vala dirs
- Remove dead errors
- Silence some build warnings for todo()! functions
- Don't attempt to version parse git upstreams for now
- Load & apply control file modifications
- Do not build the library
- Warn if version doesn't match parsed url when building
- Adapt for Qt 6.11
- Make cli subcommand non optional
- Make linters happy
- Fix error output of hash-mismatch to only show hash string
- Prefix unused variables for todo function with _
- Use CDN for the default volatile stream
- Upstream: plain: Create partial file with .part extension again
- Build: upstream: Move this module to the crate root
- Build: upstream: Split logic to fetch and store plain upstreams
- Update onboarding link in README.md
- Add build --verify flag to validate manifest
- Mount ccache config if exists
- Use volatile stream in default build profile
- Detect archive package based on upstream rename
- Add cleanup flag to build
- Implement regex-based rundep exclusions
- Canonicalize runpath
- Cache go modules as well
- Add caching for golang builds
- Annotate infer errors
- Annotate bsdtar execution errors as such
- Adjust for libstdc
- Eliminate all lingering threads
- Add time elapsed and percentage complete to ninja status
- Add undefined-version ldflag
- Adjust patch again
- Increase sleep after dropping runtime
- Adjust patch macro
- Allow packager to keep .la files
- Remove qmlcachegen macros
- Sort layout entries by name before processing
- Update git tests to add user config to test git repo
- Fix upstream ordering in yaml updates
- Refactor git upstream hash resolution to use `git rev-parse` and add more tests
- Drop versioned python deps
- Add %export_docbook_catalogs misc action
- Tweak examples in misc macros
- Add libexec to cmake macros
- Add 7z
- Allow for skipping dbginfo entirely
- Add .tgz for bsdtar-static
- Use bsdtar for `boulder new`
- Use build.aerynos.dev/volatile repository
- Add version flag
- Add `%cargo_vendor` action macro
- Fix phase timing output once and for all
- Use bsdtar-static
- Emul32 migration part 3
- Update python macros for split
- Add .gir files to devel
- Have configure_with_bash actually use bash
- Fix Qt/KDE macros
- Add CCACHE_BASEDIR to improve cache hit rates
- Fix up remaining compressman PR issues
- Fix AerynOS typo in default profile
- Make chroots less crippled
- Add Fortran support
- Remove assembler directive
- Fix a thinko in pypi metadata handling
- Adapt GNU toolchain to libstdc++ split
- Fix pattern matching
- Add `%gtk_update_icon_cache` action macro
- Also handle DT_RUNPATH in conjunction with DT_RPATH...
- Include origin dir in RPATH evaluation for pipewire/etc
- Fix handling of RPATHS to fix #366
- Set SCHED_BATCH for builds
- Cfg! compat_dlang_emul_both -> compat_dlang_emul_flush
- Update cargo_test macro as well
- Inherit root package name / summary if missing
- Combine all root & profile build / check deps
- Inherit root build if profile is missing phase
- Update cargo macros
- Hook up python providers
- Use partial download path for upstream fetching
- Use unique upstream path for reliable caching
- Use println instead of format to print
- Fix clippy lint
- Add support for sccache
- Ignore lib32 .la files
- Install `layout` as part of base packages
- Remove derpy concurrency test notes
- Hook up rust optimization flags
- Fix typo in cargo match for boulder new
- Drop environment hack for cargo now SSL works
- Tweak concurrency test build commands
- Disallow moss root
- Nuke the local-x86_64 default profile
- Update: Add progress & fix version flag conflict
- Set SOURCE_DATE_EPOCH
- Switch to `.yaml` extension by default
- Standardize version handling
- Add conflicts support
- Cli: Fix macro width alignment when width == 0
- Cli: Add macros command to print macro definitions
- Fix interpreter detection
- Always run x86 build before x64 one
- Always set manifest.bin build_release to 0
- Fix typo in [`Marcos`] in package.rs
- Emit build_release for .jsonc manifest
- Fix more clippy lints
- Add python to draft
- Add autotools to draft
- Add meson to draft
- Add new command
- Actually respect the `-o` output flag. Avalanche wants it
- Don't serialize empty lists in json manifest
- Only add layouts payload if we have some
- Don't add empty content payload
- Allow -u short flag for update
- Add --update flag to refresh repos during build
- Add more timing granularity
- Tweak Analysis Phase output to look slick
- Create README.md
- Bring data/ up to parity with boulder-d
- Add %cargo_* actions

### Boulder+moss

- Move boulder utils to moss
- Update to v0.26.1

### Boulder-data

- Use binary(*) deps instead of named ones
- Support mold linker
- Remove trivial null character
- Add flags for compressing debug symbols and enable by default
- Default to thin LTO enabled
- Add early-warning error flags for LTO
- Add fat-lto-objects flags and enable by default
- Update GCC LTO configuration
- Add build-id and enable by default

### Boulder/analysis

- Add compress man handler

### Boulder/build

- Fix runtime drops (double drop)

### Boulder/build/root

- Make `os-info` a mandatory package

### Boulder/cli

- Add/Tweak descriptions for global args
- Add short -y to existing long --yes arg
- Do not support autoupdating when first upstream is git
- Fix writing the original input path when out path differs
- Add cache {clean,size} subcommand options
- Make subcommand optional
- Add `--generate-completions` arg to generate bash/fish/zsh compl
- Add `--generate-manpages` here too
- Inhibit system from going down during build
- Print env paths in verbose mode
- Add short arg option for --upstreams

### Boulder/cli/build

- Default to `stone.yaml` path now

### Boulder/data

- Add macros for perl Module::Build
- Update BOLT optimization options
- Ensure split-machine-functions are disabled in bolt flags
- Set baseline rust flags

### Boulder/data/macros

- Rename python macro to python_setup
- Provide python_compile macro
- Initial perl macros

### Boulder/draft

- Add tests for matchers
- Always unwrap regexes
- Add matcher for gitlab URLs
- Strip v in version if 2nd char is a digit for github
- Remove infer usage
- Ensure version is always quoted when writing stone.yaml
- Ensure we don't match against empty license strings
- Support setting metadata from metacpan urls
- Support setting phases for perl modules
- Add support for license matching when running boulder new
- Add initial ruby support to boulder new
- Add metadata matching for pypi.org
- Allow transforming the given URL
- Use infer to get extension type to perform extraction
- Auto guess rss for pypi URLs
- Don't match monitoring on metadata name extract failure
- Emit `networking: true` for cargo builds
- Fix a typo python_build -> python_setup

### Boulder/draft/metadata

- Match underscores in basic first capture group

### Boulder/drafter

- Auto add cmake deps on boulder new

### Boulder/env

- Use XDG_DATA_HOME for data dir when appropriate

### Boulder/macros

- Add go mod download
- Add GOAMD64
- Improve hardening
- Add Bazel macros
- Update build triple
- Update meson macros
- Add ninja macros
- Update cmake macros
- Update polly flags
- Add Ruby macros
- Add Qt designer plugins
- Add kf6 macros
- Add qt5/qt6 macros
- Add ccache stats macros

### Boulder/macros/autotools

- Use binary deps, add missing autopoint

### Boulder/package

- Order packages in manifest alphabetically

### Boulder/stone

- Enable multithreaded compression

### Build

- Adopt FOL-style Make interface
- Inherit vfs version from workspace
- Scope active reblit route tests
- Preserve the cached development shell
- Use noninteractive Bash in dev shell
- Resolve Bash from the development shell
- Split help from root makefile
- Cover client and Mason boundary splits
- Cover split archive and package boundaries
- Add focused split regression lanes
- Cover extracted hardening modules
- Add extraction regression lanes
- Add focused client and validation tests
- Split focused hardening tests
- Preserve example listing failures
- Freeze structural toolchain commands
- Verify frozen executable bindings
- Keep manifest verification host-only
- Fingerprint executor semantics by content
- Enforce Gluon configuration boundary
- Watch relocated workspace paths
- Replace just with make

### Builder

- Make Gluon modules structurally complete

### Buildinfo

- Fail closed on unbound native lanes
- Close native tool selection gaps
- Bind dependency-selected native tools
- Bind exact tool executable identities
- Fingerprint native build context

### Cargo

- Remove frozen dependency fetch escape hatch

### Cast

- Add private device broker service
- Keep internal crate names behind the public boundary
- Consolidate tools behind one binary

### Cleanup

- Remove retired slot parking entry point

### Cli

- Add rough, preliminary `boot` subcommand
- Begin new inspect command to test the stone reader
- Hook up the error chain now to make life easier
- Add skeleton for subcommand based list
- Add skeletons for install/remove
- Reorganise CLI into module

### Cli/extract

- Add wrapper on `content` unpack

### Cli/info

- Revamp output for missing details

### Cli/inspect

- Cleanup unused import

### Cli/list

- Add local aliases (TODO: Add global)

### Cli/mod

- Cleaner use

### Cli/repo

- Plug in `repo update`

### Cli/search_file

- Ensure /usr/ prefix searches still succeed

### Client

- Bind boot namespace publication inputs
- Own retained metadata proof
- Split retained metadata inode helpers
- Stop injecting state ID into the os-release
- Bump os version
- Render progressbar for all transaction/system triggers
- Decouple moss version from os-release, unrelated
- Remove typo
- Finish error hookup
- Simplify
- Cleanup
- Add some accessor APIs
- Start fleshing out the Client API
- Add skeletal Client struct

### Client/boot

- Skip broken/missing sysroots for entries
- Further decoupling of system vs installed state
- Mount `XBOOTLDR` and `EFI` ([#284](https://github.com/bresilla/roc/issues/284))
- Skip failed topology probe in boot module (non fatal)

### Client/cli

- Move cli code to client for apply_state
- Finish `install` command ([#38](https://github.com/bresilla/roc/issues/38))

### Client/postblit

- Rework trigger scopes to allow ephemeral roots
- Reorganise to support system/transaction split
- Fix leaking visibility
- Switch trigger naming to tx/sys

### Clippy

- Fix Rust 1.80 cfg warning

### Cobble

- Return the package id for subsequent interactions

### Config

- Split Gluon fragment storage
- Harden Gluon fragment operations
- Validate complete evaluation fingerprints
- Retain evaluation root provenance
- Reject mixed-case YAML and KDL paths
- Protect authored fragments from deletion
- Remove YAML and KDL backends
- Protect authored Gluon fragments
- Add deterministic Gluon fragment manager
- Contain and fingerprint Gluon imports
- Add restricted Gluon evaluator foundation
- Add kdl support & prioritize it
- Use snafu instead of thiserror
- Allow loading all configs as vec

### Container

- Bound retired cgroup state
- Strengthen private dev live proof
- Fix sticky private dev parent
- Fix private device attachment
- Integrate brokered private devices
- Prove private minimal device provider
- Prove nested mount exclusion deterministically
- Rebind anchored inputs in child namespaces
- Add authenticated anchored locators
- Split cgroup policy and lifecycle
- Split activation and root tests
- Extract mount boundary
- Extract cgroup control files
- Extract process runtime
- Extract cgroup tests
- Bound joined task quiescence
- Normalize complete payload credentials
- Clarify and harden Linux 5.14 controls
- Enforce bounded writable scratch mounts
- Retain exact pidfd lifecycle authority
- Activate delegated cgroup controllers
- Harden namespace activation
- Enforce cgrouped payload isolation
- Isolate exact mounted objects
- Activate frozen roots by descriptor
- Bound namespace id mapping
- Make pseudo-filesystems policy-driven
- Use nc over libc for move_mount for musl support
- Bind mount via open_tree -> move_mount
- Only invoke ip when installed in the host
- Stop nuking accounts and protocols
- Allow disabling the overwrite of /etc files
- Add README.md

### Crates

- Gitwrap: Achieve REUSE compliance
- Add the "gitwrap" crate
- Add version_parse
- 2024
- Import fnmatch from usysconf-rs

### Crates/dag

- Add batched_topo() method

### Crates/triggers

- Avoid unneccessary clones in ExtractedHandler

### Crates/tui

- Allow to pretty-print a custom number or columns
- Simple refactor

### Crates/version_parse

- Fix parsing version when it isn't the last part
- Add support for GitHub downloads in VcsProviders
- Change VcsProvider.path_contains to accept an array
- Fix gitlab VCS provider parts extraction
- Refactor try_extract_vcs_url()
- Add support for codeberg URLs
- Series_version -> release_series
- Autolint fix
- Allow optional v prefix in semvar series_version
- Add support for parsing "series-version" from uri

### Dag

- Expose inner petgraph via asref
- Expose topological sort functionality
- Format
- Add exports to make Digraph usable
- Yeah, I like dags. I like copyright headers, too.

### Dag/lib

- Add this new invention: Documentation
- Typos

### Dag/subgraph

- Doc

### Data/macros

- Add missing python dep for pyproject.toml macros

### Database

- Bound deterministic boot inputs

### Db

- Preserve exact removal causality

### Deps

- Update

### Derivations

- Bound frozen process data

### Dev

- Pin fixture host toolchain
- Include ripgrep in flake shell
- Add Nix development shell

### Downloads

- Preserve transport across redirects
- Bound and verify fetched bytes

### Draft

- Stop nightly clippy making me look bad

### Examples

- Declare a prebuilt ELF runtime contract
- Add declarative source and runtime patterns
- Execute external test vectors
- Add external test vectors
- Add locked template substitution
- Compose a declarative userspace profile
- Execute source-less generated configuration
- Add typed userspace composition
- Pin contentful Stone bootstrap
- Verify offline source admission
- Add locked offline execution sources
- Gate the declarative package corpus
- Execute minimal frozen derivation
- Verify Gluon corpus through Cast
- Add Nix-inspired Gluon package corpus

### Executor

- Bound frozen step resources
- Contain every build step descendant
- Reset caches before frozen builds

### Fetch/request

- Add missing spdx headers

### Fnmatch

- Fix `**` glob not matching forward slash
- Fix invalid wide matches by anchoring
- Fix failures for doc tests
- Support tokio usage with Clone
- Add missing documentation
- Implement relevant deserialise/ord/eq traits

### Forge

- Wire active reblit coordinator
- Finalize active reblit boot transaction
- Hand off active reblit completion
- Hand off active reblit commit cleanup
- Commit active reblit boot publication
- Hand off active reblit boot staging
- Complete active reblit no-boot recovery
- Admit active reblit no-boot cleanup
- Hand off active reblit no-boot commit
- Recover system trigger rollback phases
- Retain reblit identity through system triggers
- Share candidate metadata policy
- Close uncertain boot repair safely
- Prove boot cleanup restart seams
- Resume promoted boot cleanup after restart
- Retain promoted boot restart authority
- Bind restart cleanup to live boot targets
- Keep promoted boot sync forward-only
- Derive restart boot cleanup plans
- Clean promoted boot publication residue
- Reject replacement pair drift before promotion
- Execute classified boot publication deltas
- Derive boot receipt claims during staging
- Bridge owned boot output replacement
- Assess boot leaves without mutation
- Seal live boot delta classification
- Load the current installed boot receipt chain
- Replace boot outputs with recoverable exchanges
- Derive authenticated boot publication deltas
- Authenticate promoted boot receipt chains
- Retain installed boot publication receipt
- Finalize forward active reblit
- Complete active reblit receipt cleanup
- Retire promoted boot receipt head
- Persist active reblit cleanup completion
- Make active reblit cleanup durable
- Authenticate active reblit commit cleanup
- Adopt boot completion during startup
- Authenticate boot completion at startup
- Authenticate promoted boot receipts at startup
- Persist exact boot sync completion
- Bind boot completion to journal deadlines
- Promote terminal boot publication evidence
- Promote exact boot publication receipts
- Attempt receipt-bound boot publication
- Preflight aggregate boot publication
- Retain boot publication targets
- Retain staged boot publication inputs
- Retain staged boot publication receipt
- Retain nested boot publication parents
- Publish one retained boot payload
- Stage bound boot sync receipts
- Persist complete boot publication receipts
- Correlate boot publication receipts
- Separate frozen discard traversal
- Prove RootLinks terminal restart recovery
- Finalize RootLinks ActiveReblit rollback
- Finalize RootLinks NewState rollback
- Recover interrupted journal deletion
- Finalize RootLinks archived rollback
- Bind terminal journal deletion
- Complete RootLinks NewState exactly
- Prove RootLinks invalidation across SIGKILL
- Invalidate RootLinks NewState exactly
- Bind RootLinks archived completion exactly
- Bind RootLinks NewState route exactly
- Preserve RootLinks candidates exactly
- Bind ActiveReblit candidate persistence exactly
- Bind archived candidate persistence exactly
- Bind NewState candidate persistence exactly
- Bind candidate preservation to exact record
- Complete exact root ABI rollback reversal
- Route durable root ABI rollback exactly
- Recover durable root ABI completion
- Persist root ABI completion
- Normalize interrupted root ABI publication
- Anchor system trigger views
- Route completed boot repair journal
- Admit inert shebang padding
- Retain stateful system trigger discovery
- Split direct client tests by behavior
- Split retained filesystem operations
- Split cache authentication and publication
- Split database adapters and tests
- Split installation controls and tests
- Stabilize extracted hardening tests
- Split transition journal storage and tests
- Split repository manager
- Extract transition journal contract
- Bound read-only query admission
- Add retained read-only client
- Add bounded read-only database snapshots
- Retain read-only installation snapshots
- Harden archived-state pruning
- Load default intent after startup gate
- Keep trigger preparation descriptor rooted
- Pin ephemeral trigger authority
- Retain ephemeral metadata authority
- Keep metadata hook warning free
- Retain candidate metadata through activation
- Preflight root ABI before activation
- Gate live discovery on recovery evidence
- Retain live state and trigger authority
- Retain writable materialization capabilities
- Prevent frozen startup gate bypass
- Gate system client startup
- Retain frozen activation anchor
- Harden inactive archived state repair
- Preserve active reblit wrappers atomically
- Retain archived candidates across wrapper moves
- Archive previous trees through retained slots
- Exchange retained state trees by descriptor
- Quarantine frozen roots through retained parents
- Reserve recovery for frozen publication
- Publish frozen roots through retained parents
- Prove frozen roots against declarative trees
- Timestamp retained filesystem capabilities
- Bind state transitions to durable tree identities
- Publish durable tree identities
- Publish state metadata atomically
- Reserve system metadata in frozen layouts
- Harden installation provisioning
- Isolate rootless extraction state
- Authenticate descriptor chmod on Linux 5.6
- Preserve cache roots during parallel cleanup
- Generate transition identifiers securely
- Make journal tree identity reboot-aware
- Add durable transition journal
- Audit transition database evidence
- Harden merged-usr root links
- Correlate fresh states with transitions
- Authenticate cross-package executable links
- Recover failed atomic state transitions
- Bound trigger process supervision
- Enforce canonical usr-only layouts
- Fail closed on trigger errors
- Provision secure cache roots
- Authenticate exact assets during materialization
- Retain and publish authenticated cache files
- Materialize metadata-only frozen closures
- Publish and guard frozen roots atomically

### Fs

- Capture mounted boot topology evidence
- Model mounted boot topology facts
- Preserve boot intent deadlines
- Preserve mounted topology deadlines
- Authenticate current-thread mountinfo
- Preserve caller sysfs deadlines
- Retain task-rooted attachment chains
- Bind exact mountinfo attachments
- Retain current mount namespace and task root
- Retain authenticated sysfs partition identity
- Bound sysfs parsing through completion
- Parse bounded sysfs block identity
- Bound pseudo-filesystem identity reads
- Parse mountinfo within fixed bounds
- Bound installation root revalidation
- Bound runtime evidence reads

### Git

- Pin cache lock and install cleanup
- Require stable quota inventories
- Harden private mirror transactions
- Seal bounded checkout materialization
- Bound source acquisition resources

### Gitwrap

- Split runtime supervision and tests
- Extract repository filesystem
- Clone frozen sources without checkout

### Gluon

- Anchor source reads to trusted descriptors
- Bound the complete evaluation pipeline

### Gluon_config

- Lock test codegen dependency
- Cover typed diagnostic boundaries
- Cover network and clock denial

### Hardening

- Bound CLI state requests
- Reject malformed package requests
- Make self-upgrade fail closed
- Reject usr extended attributes

### Headers/versioning

- Expose version() properties, public members

### Identity

- Bound tree marker reads

### Installation

- Add some documentation, fix import

### Integration

- Ship only the cast executable

### Journal

- Bind successors across canonical reopen
- Bind conditional record advances
- Model verified boot repair completion
- Capture authenticated runtime evidence

### Justfile

- Exclude license-list-data/ from `just fix`
- Exclude license-list-data/ from typos run
- Improve readability
- Get-started: Populate ~/.config/boulder directory
- Make `just get-started` POSIX sh compliant
- Make get-started slightly more ergonomic
- Switch to using default XDG_*_HOME dirs
- Add onboarding link to docs.serpentos.com
- Add `just get-started` and update README
- Switch default target to onboarding
- Switch to packaging target by default
- Document the default target
- Add build helpers, `lint` and `fix` targets for local development

### Libstone

- Size record storage by record type
- Avoid digest buffer overflow

### Lint

- Allow the OpenType loca table
- Admit historical commit identifiers

### Linux-fs

- Accept opaque mountinfo roots
- Stream sealed boot namespace sources
- Bind retained boot namespace attachment
- Bind retained devtmpfs GPT authentication
- Retain assessed namespace root identity
- Observe retained boot namespace
- Require owned GPT parent authentication
- Retain authenticated GPT parent node
- Bind retained devtmpfs attachment
- Authenticate retained devtmpfs descriptors
- Authenticate retained GPT reads
- Bound namespace observer ownership
- Add read-only block and directory syscalls
- Harden boot adapter foundations
- Bind GPT composition metadata
- Retain stable GPT table identity
- Authenticate exact devtmpfs policy
- Seal sysfs GPT device expectations
- Classify boot destination namespaces
- Authenticate strict GPT partition roles
- Retain partition geometry evidence
- Parse partition geometry evidence
- Retain authenticated block device names
- Validate kernel block device names
- Bind boot evidence to retained attachments
- Authenticate boot filesystem descriptors
- Add one-shot descriptor chmod
- Add one-shot mkdir adapter

### Lock

- Separate target identity and validate reuse

### Locks

- Bound and pin generated Gluon reads

### Macros

- Document the %(cargo_target_dir) definition
- Quick update for docs site

### Main

- No need for dyn error anymore
- Add `-v`, `--version`
- Exit `unreachable` for unknown args
- Add `version` processing
- Use `clap` to define our main entry point

### Mason

- Distinguish shared libraries from loaders
- Expect namespaced plugin providers
- Harden bootstrap fixture boundaries
- Preflight ELF debug routes before mutation
- Preflight generated artifact routes
- Harden frozen packaging handoff
- Split package contract tests
- Split frozen executor supervision
- Split retained workspace paths
- Split Git tree materialization
- Split frozen bundle publication
- Split authenticated archive extraction
- Split transactional package emission
- Split analyzer boundary and tests
- Split collection transactions
- Split package collection engine
- Execute typed build-tree programs
- Expand contentful builder fixtures
- Split planner by responsibility
- Split bundle tests by behavior
- Split bootstrap tests by behavior
- Split planner tests by behavior
- Split container module by function
- Preflight delegated execution
- Bound execution lock waits
- Add typed output policy example
- Add factory override execution fixture
- Prove Gluon factory composition semantics
- Clean caches through retained capabilities
- Normalize archive directories by descriptor
- Retain authenticated workspace roots
- Reserve Cast system metadata paths
- Run execution fixtures in delegated units
- Require delegated build cgroups
- Pin the complete frozen build boundary
- Retain build modules across the crate move

### Moss

- Materialize deterministic frozen roots
- Seal postblit process inputs
- Isolate ephemeral package assets
- Report dependency cycles
- Install exact package closures
- Cover Gluon snapshots across state recovery
- Persist authored intent provenance
- Shrink top-level CLI errors
- Box Gluon configuration errors
- Separate authored intent from state snapshots
- Suggest Gluon repository updates
- Manage repositories through Gluon
- Load packaged triggers from Gluon
- Replace KDL system model core with Gluon
- Add Gluon repository configuration codec
- Evaluate versioned Gluon system intent
- Encode canonical Gluon system snapshots
- Always validate CAS files during unpack
- Verify CAS hash before skipping unpack when already exits
- Verify CAS file hashes during unpack
- Validate download hash when checking if cached
- Validate downloaded binary stone SHA256
- Fix `state prune` partial path resolution
- Unpack CAS files to `.part` w/ atomic rename
- Fix repo refresh for system model
- Richly handle system-model outdated repo update
- Expose method to resolve history index from root index
- Add sync bounds to request reader
- Add initial self upgrade flow
- Enable adding / listing root index repos
- Detect unsupported repo format & handle error at entrypoint
- Add arch to repo root index options
- Decode repo root index options via KDL system model
- Cache & load root resolved index uri
- Define repo as direct index vs root index options & resolve
- Add initial repo format root index serde types
- Get rid of unnecessary `collect` in a `flat_map` invocation in boot sync
- Add testing flag to dev-deps
- Rework test suite to use "testing" feature
- Add addtl. progress output context on prune
- Use par_remove_dirs_all() when removing state archive folders
- Add simulate support to install()
- Add simulate support to remove()
- Add simulate support to sync()
- Add our own {par_}remove_dir_all() impls in utils
- Ignore NotFound error if dir doesn't exist when fixing state
- Request: write_to_file: Delete partial file if any error occurred
- Use composable download methods
- Support removing more than one arbitary state
- Fix user-visible TODO comment in verify cli code
- Change blitting paths progress from info to trace
- Fix initrd service with newer Dracut
- Add flag to disable system-model warning
- Move ensure dir exists to client method
- CLI option w/ default_value already in help
- Detach blitting code from Client
- Split cache::Error in two and use snafu for them
- Fix uncaught typo (codespell doesn't catch it)
- Synchronize boot during moss state prune
- Add moss boot sync command
- Synchronize boot during moss state activate
- Add more context to client caching errors
- Make the system-model preamble fit in 80x24
- Allow repo update w/ system-model
- Implement RAII guard for unpacking progress
- Tweak system-model preamble slightly
- Display added vs upgraded packages on sync output
- Add --import flag for moss sync
- Add --output flag for moss state export
- Add system-model preamble to cli
- Add state export command to export system-model
- Record /usr/lib/system-model.kdl during fstxn blit
- Add functions to encode & update system-model
- Limit repo commands when system-model is used
- Add cache prune command
- Fix bug from 03c348a1, hook up cache dir
- Cache transaction selection providers
- Remove orphaned deps during `moss remove`
- Parallelize state verify
- Use functional style for issue collection
- Simplify asset grouping in state verify
- Correct display of build release in package column output
- Add version flag
- Add path as context to index::Error::StoneWrite
- Abort index if there's a .stone file with non-utf8 path
- Fix tx provider resolution order for sync ([#514](https://github.com/bresilla/roc/issues/514))
- Fix clippy lint.
- Add filesystem baseline performance data
- Cli: Add blit stats
- Verify: query installdb directly
- Add basic scaffolding for upcoming varlink integration
- Inhibit system whilst applying states
- Add ability to disable repositories
- Left align search output for easier readability
- Fix missing gap on largest item
- Improve DB batching
- Use sqlite exlusive transactions
- Fix duplicate inserts
- Vfs: Cache expensive path calc / allocations
- Disable automatic referer header in reqwest::ClientBuilder
- Simplify concurrent download + unpack
- Ignore SIGINT during blit
- Prune: Remove partial downloads
- Download packages to partial file then rename
- Allow sync on installed pkg missing from repos
- Only acquire lockfiles when mutable access
- Disallow multiple moss clients per root via lockfile
- Standardize version handling
- Insert conflicts correctly and add a conflicts test
- Add support for storing conflicts
- Verify: Add prompt confirmation before fixing issues
- Verify: Finalize TUI output
- Verify: Cleanup dirty staging dir after fixing active state
- Add verify command
- Fix clippy lint, we don't need a len helper
- Search: Update column output to properly right align text
- Search: Fix panic when no results returned
- Meta db: Simplify querying 1:N meta relations
- Add a bunch of documentation (more to come)
- Cli: Allow to search packages by a keyword
- Add API to search for packages by a keyword
- Allow unpacking binary stone w/out layouts
- Handle caching package w/out content payload
- Add update flag to sync
- Add TUI output when refreshing repos
- Add install timings
- Add a CACHEDIR.TAG to `.moss/cache`
- Integrate a new `postblit` step for `pre-triggers`
- Make use of dialoguer instead of clunky ask_yes_no
- Require vfs
- Add ActivePlugin (currently borky)
- Add repository + cli support for listing repos
- Ensure we build `stone` directly into our binary

### Moss/Cargo

- Require local `dag` crate for deps

### Moss/boot

- Adapt to new blsforme by loading snippets

### Moss/cli

- Convert install() to use clap derive structs
- Convert remove() to use clap derive structs
- Use new extract function in moss client
- Remove state diff support for now
- Add state query and state diff subcommands
- Add support for search-file
- Better "branding"
- Add `--generate-completions` for zsh, fish, bash
- Add support for `--generate-manpages`
- Introduce non-functioning Upgrade command
- Add boilerplate for DB merge in install
- Migrate `pretty.rs` to a tui ColumnDisplay trait
- Use name_to_provider mapper for install/info
- Add -D and -y global arguments
- More hookup for extract
- Add placeholder for extract command
- Add info skeleton

### Moss/cli/extract

- Use pkg ID as the extraction dir
- Remove doc comment for reference
- Finish up initial extraction
- Fix typo
- Cleaner imports
- Minor cleanups
- Preliminary test code for extracting indices from content

### Moss/cli/info

- Improve word-wrapping functionality
- Move homepage up in info
- Pretty print information now
- Support basic query of pkgs

### Moss/cli/inspect

- Remove random unused import
- Layout in layouts
- Unfudge output to use columns
- Fix imports
- Use `pkgconfig(name)` style display strings
- Ugly-print the Dependency/Provider kinds
- Emit Dependency/Provider properly
- Emit the layouts encountered
- Hax to emit raw encoded data
- Prove (roughly) inspect works
- Port to latest stone reader API changes

### Moss/cli/install

- Remove useless eprintln
- FIx silliness in resolving package names
- Ask yes/no before installing packages, respect `-y`
- Call out to blit_root
- Record new state when done
- Drop call to set_length
- Improve progressbar feedback
- Hook the progressbar up to our iterator
- Iplement chunked batch additions of LayoutDB
- Use transaction resolution now for deps!
- Use pretty.rs to pretty print as columns
- Functional style to collect (async) all pkgs/errors
- Add parsing lookup to find all input candidates

### Moss/cli/list

- Dedupe to mask lower priority plugins
- Don't change style on list available
- Fix emission to match column display
- Simplify, make clippy happy
- Render like old moss (with crossterm dep)
- Add aligned rendering of items
- Add placeholders for implementation

### Moss/cli/pretty

- Render columns based on real terminal width
- Better alignment of values
- Organise into alphasorted columns

### Moss/cli/remove

- Stop using "unimplemented" error

### Moss/cli/repo

- Add `--priority` flag (boulder support)
- Support overriding `-c`omment of repos
- Scaffolding to support repo removal
- Missing docs
- Pretty print, sort by priority
- Reorganise into subcommands, add docs

### Moss/cli/state

- Emit creation timestamp with local timezone ([#346](https://github.com/bresilla/roc/issues/346))
- Nicer output in general

### Moss/client

- Better handle if there are no contained layouts
- Remove arc / rwlock from parallel blitting
- Blit entries in parallel
- Generate os-release based on os-info data
- Emit filesystem transaction ID to enable future rollbacks
- Forbid linking of empty files ([#372](https://github.com/bresilla/roc/issues/372))
- Attempt speed up by parallel emission of child adjacents
- Better hardcoded version >_>
- Fix versioning for 0.5
- Introduce preliminary boot integration (unmanaged)
- Add our own renameat2 wrapper as musl lacks one
- Stop making users download more RAM
- Fix installation of `/` forwarding links
- Render a progressbar for blitting
- Use 00755 permissions (octal) for default dirs
- Close directory file descriptors when done
- Provide a root fs layout automatically
- Implement the staging blit
- Add unfinished "blit files" routine
- Initialise the required databases
- Shorthand initialisation
- Actual commit I meant to push, see previous commit for confusion.
- Allow borrowing the Registry
- Integration of Registry
- Use the Installation

### Moss/client/boot

- Simple port to new blsforme for now
- Initial working integration of blsforme

### Moss/client/cache

- Fix race condition in file blits

### Moss/db

- Stop using deprecated APIs for UTC timestamps
- Modify for get_providers lookup

### Moss/db/layout

- Add query for per-id lookups

### Moss/dependency

- Fix doc links
- Fix doc test to actually work

### Moss/installation

- Some docs

### Moss/package

- Add Default to empower blits
- Implement hash/ord for use in HashSet + sorting

### Moss/package/meta

- Port the ID from MetaPayload

### Moss/package/render

- Fix off by one in rendering gaps
- Include the release number
- Rabbits

### Moss/registry

- Remove stray `dbg` left in repository plugin
- Restrict removal lookups to installed candidates
- Some skeleton code to support fetch_item
- Skeletal `Job` type
- Use a full set rather than individually digraph incoming

### Moss/registry/job

- Improve naming

### Moss/registry/transaction

- Only consume first element from streams
- Remove unused database
- Implement graph dependency resolution
- Add skeleton (pre-graph) code

### Moss/request

- Add a `todo` for `file://` URIs (indices, etc)

### Moss/search

- Remove headers from search results
- Refactor handle() method
- Set up grouped matches
- Add argument to `--provides` flag
- Setup testing fixture
- Highlight matches in green for seach output
- Add simple test suite
- Implement From<Package> for Output

### Moss/signal

- Implement inhibit wrapper to inhibit system going down

### Moss/transaction

- Boilerplate for the transaction type (as yet unused)

### Moss/util

- Rewrite ignore_notfound wrapper

### Moss/{db,dependency}

- Add missing docs

### Nightly.yaml

- Switch back to known working version
- Fiiine, be a pedant.
- Where did I come from? Why am I here?
- Just show me the versions ok?
- Make it build faster while testing
- More RUSTFLAGS experiments
- Attempt to fix RUSTFLAGS quoting derp
- Force ld.lld clang linker from $PATH

### Onboarding

- Don't override dev target debug setting

### Package

- Publish analyzer artifacts transactionally
- Add transactional file replacement
- Require execution lock for packaging
- Bind manifest verification to publication
- Seal derivation bundle publication
- Seal emitted artifact set
- Clarify analyzer restat comment
- Satisfy admission lint
- Seal read-only analyzer inputs
- Preserve Python dependency extras
- Seal bounded output inventory
- Bound output collection
- Contain analyzer descendants
- Fail closed on ELF tool errors
- Publish only declared artefact bundles
- Fail closed on analyzer processes
- Reject ambiguous profile identities
- Keep output metadata declarative
- Reject unsafe artifact identities
- Reject impure frozen networking
- Execute only frozen analyzers
- Remove duplicated standard builder tools
- Make default outputs pure Gluon data

### Payload/meta

- For v1, we must continue to emit strings with \0

### Plan

- Record completed finite boundaries
- Record verified offline source fixtures
- Require real builder execution
- Record policy hardening
- Freeze structural executable capabilities
- Freeze analyzer and credential capabilities
- Identify the selected structural builder
- Preserve structural package hooks
- Freeze root materialization mode
- Freeze complete configuration provenance
- Make analysis and outputs authoritative
- Freeze policy-owned sandbox layout
- Bind derivations to executor content
- Freeze typed package relations
- Validate execution semantics before freeze

### Planner

- Retain validated target identity
- Freeze every Gluon package example
- Preserve typed input provenance
- Bind identities to typed policy

### Policy

- Split rooted tuning modules and enforce source LOC
- Bound declarative expansion
- Declare ordered package analyzers
- Remove unconsumed declarative fields
- Reject unsupported artifact architectures
- Diagnose retired build targets
- Compose configured Gluon layers
- Validate authoritative target names
- Add ordered layer manifest ABI
- Add total build policy patches
- Remove obsolete archive tool rules
- Type PGO completion commands
- Preserve typed environment semantics
- Expose typed source provenance
- Declare root source and target inputs
- Migrate the typed tuning catalog
- Load typed repository build policy
- Add typed Gluon build policy
- Remove obsolete action modules

### Postblit

- Cargo fmt

### Recovery

- Admit verified boot repair completion
- Revalidate canonical journal reopen
- Route ActiveReblit boot repair
- Bind mutable system capabilities
- Retain boot database capabilities
- Kill ActiveReblit wrapper exchange boundaries
- Kill NewState candidate move boundaries
- Kill archived preservation durability boundaries
- Prove archived post-move SIGKILL restart
- Prove archived terminal SIGKILL recovery
- Finalize archived rollback
- Production-wire archived completion
- Production-wire archived candidate preservation
- Seal ActivateArchived rollback foundations
- Prove ActiveReblit terminal process death
- Finalize ActiveReblit rollback journals
- Route ActiveReblit rollback completion
- Dispatch ActiveReblit candidate preservation
- Admit ActiveReblit candidate preservation leaf
- Persist ActiveReblit candidate preservation
- Seal ActiveReblit post-exchange durability
- Seal ActiveReblit wrapper exchange
- Require private ActiveReblit reservation
- Finalize new-state rollback at startup
- Finalize terminal rollback journal safely
- Seal rollback finalization authority
- Dispatch new-state rollback suffix
- Route invalidated database to rollback completion
- Persist fresh database invalidation
- Seal fresh database invalidation effect
- Route preserved candidates to db invalidation
- Seal fresh database invalidation routing
- Persist durable candidate preservation
- Make candidate preservation post-move durable
- Require target durability before candidate move
- Make candidate target normalization durable
- Admit disjoint target preparation leases
- Model candidate target preparation prefixes
- Require private candidate target
- Recheck evidence before candidate move
- Admit candidate preservation evidence
- Route usr restored to candidate intent
- Harden reverse dispatch interruption matrix
- Prove reverse dispatch restart convergence
- Dispatch one reverse phase at startup
- Persist exact usr restored outcome
- Seal reverse exchange durability
- Isolate one-shot reverse exchange
- Seal reverse effect handoff
- Normalize reverse usr exchange proof
- Seal usr rollback reverse admission
- Define rollback outcome provenance
- Persist exact rollback resume route
- Complete usr exchange parent durability
- Normalize authorized reblit residue
- Require retained isolation ABI before triggers
- Enforce active reblit startup topology
- Seal active reblit trigger readiness
- Authenticate startup activation namespace
- Execute journaled usr exchange once
- Bind metadata provenance to coordinator
- Store immutable state metadata provenance
- Persist usr exchange intent
- Distinguish active reblit candidate identity
- Verify archived metadata without mutation
- Bind metadata proof to coordinator
- Separate neutral metadata proof
- Sequence internal transaction triggers
- Assess pending startup evidence
- Classify persisted startup direction
- Establish durable transition coordinator prefix
- Seal candidates before journal publication
- Retain startup authority and construct journal phases

### Registry

- Absorb lifetime bound Transaction type w/ DB
- Support sideload of Meta into Cobble

### Registry/plugin

- Fix pkg_installed

### Registry/transaction

- Fix temporary reference which is unnecessary

### Repo

- Use runner LLVM 18

### Repository

- Pin operations to index generations
- Record active index snapshot atomically
- Harden index metadata ingestion

### Runtime

- Seal frozen derivation execution

### Sandbox

- Seal frozen build roots read-only

### Serpent-style

- Sync with git for typo fixes (>_>)
- Sync

### Sources

- Harden offline Git materialization
- Enforce secure remote URL policy
- Canonicalize frozen materialization identity
- Harden frozen materialization
- Prepare validated materialization names
- Harden Git destination naming
- Lock normalized Git materializations
- Freeze structural preparation steps

### Startup

- Validate complete boot receipt state
- Authenticate boot receipt recovery

### Stone

- Expose reader module to cbindgen
- Format extracted reader fixture
- Split bounded reader implementation and tests
- Bound untrusted archive decoding
- Centralize package relations
- Decode unknown variants
- Add nul byte to provider / dependency encoding
- Add preliminary Reader
- Split headers for v1 + versioning
- Add first port of libstone headers (Agnostic)

### Stone/headers/v1

- Docs

### Stone/payload

- Begin adding docs
- Split out attribute.rs
- Split meta into meta.rs
- Split out index.rs
- Move layout into its own file
- Add missing SPDX header
- Branch off into a module
- Do not need exhaustive match here
- Remove unused import
- Document the -1 approach
- Add fixed mappings for DependencyKind printing
- Fix decoding of Dependency/Provider with the right Kind
- Add strong type for FileType in LayoutEntry

### Stone/payload/layout

- Sanitize `\0` from strings
- Address nit, `Entry` not `LayoutEntry`
- Use enum matching to improve handling

### Stone/read

- Fix build failure in test

### Stone/write/zstd

- Missing doc strings

### Tooling

- Install only Gluon configuration data

### Transaction

- `cargo fmt`

### Triggers

- Remove YAML deserialization
- Fix clippy lint
- Actually respect trigger execution order
- Fix up into initial usable form
- Introduce basic Manager type to pull in all handlers
- Introduce serde yaml format for Triggers

### Tui

- Suspend indicatif & print normally
- Pretty: Remove arbitrary padding now that we constrain to max_width
- Pretty: Prevent panic when items are empty
- Pretty: Add max width constraint to prevent column overflow
- Drop ask_yes_no in favour of dialoguer
- Use same line for prompt response
- Add helper to ask yes/no question as bool return
- Nuke examples
- Add new term_size() helper to get sane fallback constraints
- Cargo fmt
- Absorb the crossterm dep
- Add missing docs

### Tui/pretty

- Don't use reserved `print` name, breaks cargo doc
- Remove now unused State mapping object

### Tui/widget

- Rename to mod for consistency

### Util

- Add function to remove a set of dirs recursively parallelized

### Varlink

- Remove half-arsed impl until ready, stoopid CI errors
- Fix irritating clippy warning for autogen code

### Vfs

- Add Kind::is_directory
- Use snafu instead of thiserror
- Format clean
- Make the errors for duplicate entries more useful
- Support len/is_empty for enumeration helpers
- Docfix
- Expose a DFS iterator for the tree
- Fix test suite for now, apply cargo clippy help

### Vfs/tree

- Add structured recursive element nodes
- Remove unused `Node`

### Workspace

- Consolidate support files under misc
- Move libstone under crates
- Move applications under bin
- Fix clippy lints and remove dead code

### Yaml/updater

- Always emit trailing newline ([#298](https://github.com/bresilla/roc/issues/298))

