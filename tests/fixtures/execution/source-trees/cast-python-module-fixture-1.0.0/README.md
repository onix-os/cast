# Cast Python module fixture

This small package is used only to prove Cast's offline PEP 517 build,
installation, runtime-relation, Stone decoding, and reproduction boundaries.
It has one explicit runtime dependency and never contacts a Python package
index during its build.
