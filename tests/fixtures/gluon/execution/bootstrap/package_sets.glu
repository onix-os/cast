let aggregate = import! "./aggregate_package_ids.glu"
let build_systems = import! "./build_system_package_sets.glu"
let specialized = import! "./specialized_package_sets.glu"
let system_integration = import! "./system_integration_package_set.glu"
let tooling = import! "./tooling_package_sets.glu"

{
    all = aggregate,
    autotools = build_systems.autotools,
    cargo = build_systems.cargo,
    cmake = build_systems.cmake,
    cmake_zlib = build_systems.cmake_zlib,
    custom = specialized.custom,
    desktop_integration = specialized.desktop_integration,
    font_family = specialized.font_family,
    generated_config = specialized.generated_config,
    gettext_localization = specialized.gettext_localization,
    go_module = specialized.go_module,
    hooks_patch = tooling.hooks_patch,
    meson = tooling.meson,
    multiple_sources = build_systems.multiple_sources,
    pgo_workload = specialized.pgo_workload,
    python_module = specialized.python_module,
    relation_policy = specialized.relation_policy,
    system_integration_assets = system_integration,
    userspace_profile = specialized.userspace_profile,
}
