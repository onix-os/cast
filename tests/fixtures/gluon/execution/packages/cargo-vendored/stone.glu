
let a = import! cast.authored.v1

{
    meta = {
        pname = "cast-cargo-vendored-fixture",
        version = "1.0.0",
        release = 1,
        homepage = "https://fixtures.invalid/cast-cargo-vendored-fixture",
        license = ["MPL-2.0"],
    },
    // `.cargo/config.toml`, Cargo.lock, and the complete directory source are
    // part of the source archive. The structural builder adds `--frozen` and
    // has no fetch phase, so the declared dependency must resolve offline.
    builder = a.builder.cargo {
        features = [],
        binaries = ["cast-cargo-vendored-fixture"],
        run_tests = a.true,
    },
    sources = [
        a.source.archive_with {
            url = "https://fixtures.invalid/sources/cast-cargo-vendored-fixture-1.0.0.tar.gz",
            hash = "f4e4c01bcd965a8843ec3d93f8d72e3c85d8dbf3e7b9625e17a5bffe9d255c22",
            rename = a.optional.set "cast-cargo-vendored-fixture.tar.gz",
            strip_dirs = a.optional.set 1,
            unpack = a.true,
            unpack_dir = a.optional.set "cast-cargo-vendored-fixture",
        },
    ],
    native_build_inputs = [],
    build_inputs = [],
    check_inputs = [],
    outputs = a.outputs.default,
    options = a.unset,
    profiles = [],
    architectures = [],
    tuning = [],
    emul32 = a.false,
    mold = a.false,
    hooks = a.unset,
}
