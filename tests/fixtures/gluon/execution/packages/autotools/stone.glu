
let a = import! cast.authored.v1

{
    meta = {
        pname = "cast-autotools-fixture",
        version = "1.0.0",
        release = 1,
        homepage = "https://fixtures.invalid/cast-autotools-fixture",
        license = ["MPL-2.0"],
    },
    builder = a.builder.autotools { flags = [], run_tests = a.true },
    sources = [
        a.source.archive_with {
            url = "https://fixtures.invalid/sources/cast-autotools-fixture-1.0.0.tar",
            hash = "cc72bc585ef513808277602603a6d883d0d9b8722a84e43fe65090035accf394",
            rename = a.optional.set "cast-autotools-fixture.tar",
            strip_dirs = a.optional.set 1,
            unpack = a.true,
            unpack_dir = a.optional.set "cast-autotools-fixture",
        },
    ],
    native_build_inputs = [a.dep.binary "autoreconf"],
    build_inputs = [],
    check_inputs = [],
    outputs = a.outputs.default,
    options = a.unset,
    profiles = [],
    architectures = [],
    tuning = [],
    emul32 = a.false,
    mold = a.false,
    hooks = a.some.hooks (a.hooks {
        // Regenerate the build system from authored Autoconf/Automake inputs.
        // The program binding and native dependency are both explicit so the
        // frozen plan retains the executable and dependency-role origins.
        pre_setup = [
            a.step.run (a.program.binary "autoreconf") ["-fi"],
        ],
        .. a.empty.hooks
    }),
}
