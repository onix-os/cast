
let a = import! cast.authored.v1

{
    meta = {
        pname = "cast-meson-fixture",
        version = "1.0.0",
        release = 1,
        homepage = "https://fixtures.invalid/cast-meson-fixture",
        license = ["MPL-2.0"],
    },
    builder = a.builder.meson {
        flags = [],
        run_tests = a.true,
    },
    sources = [
        a.source.archive_with {
            url = "https://fixtures.invalid/sources/cast-meson-fixture-1.0.0.tar",
            hash = "3119d25a8f56dc208c5dcc907b6c5a4203c87c7ad28897b85e2c0eeed28ab995",
            rename = a.optional.set "cast-meson-fixture.tar",
            strip_dirs = a.optional.set 1,
            unpack = a.true,
            unpack_dir = a.optional.set "cast-meson-fixture",
        },
    ],
    native_build_inputs = [],
    // The installed program must compile and link through Meson's pkg-config
    // dependency path. A separate, non-installed test helper invokes `file`
    // only while `meson test` runs, making the check-only role observable.
    build_inputs = [a.dep.pkgconfig "zlib"],
    check_inputs = [a.dep.binary "file"],
    outputs = a.outputs.default,
    options = a.unset,
    profiles = [],
    architectures = [],
    tuning = [],
    emul32 = a.false,
    mold = a.false,
    hooks = a.unset,
}
