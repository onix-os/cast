let a = import! cast.authored.v1

{
    meta = {
        pname = "cast-post-install-smoke-test-fixture",
        version = "1.0.0",
        release = 1,
        homepage = "https://fixtures.invalid/cast-post-install-smoke-test-fixture",
        license = ["MPL-2.0"],
    },
    builder = a.builder.cmake { flags = [], run_tests = a.true },
    sources = [
        a.source.archive_with {
            url = "https://fixtures.invalid/sources/cast-post-install-smoke-test-fixture-1.0.0.tar",
            hash = "76796dcbea00da45083634a3784f6a3ced54d1445f58f4777ebc2d8dc01c4a94",
            rename = a.optional.set "cast-post-install-smoke-test-fixture.tar",
            strip_dirs = a.optional.set 1,
            unpack = a.true,
            unpack_dir = a.optional.set "cast-post-install-smoke-test-fixture",
        },
    ],
    native_build_inputs = [],
    build_inputs = [],
    check_inputs = [],
    outputs = a.outputs.default,
    options = a.unset,
    profiles = [],
    architectures = [],
    tuning = [],
    emul32 = a.false,
    mold = a.false,
    hooks = a.some.hooks (a.hooks {
        // The probe validates argv[0] against these same staged-root values.
        // A build-tree executable or host PATH substitute therefore fails.
        post_install = [
            a.step.shell r#""${CAST_INSTALL_ROOT}${CAST_BINDIR}/staged-probe" --self-test"#,
        ],
        .. a.empty.hooks
    }),
}
